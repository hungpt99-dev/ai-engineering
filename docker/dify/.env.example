# Dify Docker — example env (for real deployment replace docker-compose.yml with upstream)
# See https://docs.dify.ai/en/self-host
# When you vendor the official compose, this file becomes its .env.

# Example:
# CONSOLE_API_URL=
# APP_API_URL=
# DB_USERNAME=postgres
# DB_PASSWORD=difyai123456
